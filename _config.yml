# GitHub Pages configuration for Vue.js build
# Jekyll is disabled via .nojekyll

theme: null
  webp: image/webp
  json: application/json

# Build settings
markdown: kramdown
plugins:
  - jekyll-seo-tag
